# Userspace packages verified with the TensorRT-LLM 1.3.0rc20 image.
#
# PyTorch, TorchVision, Triton, CUDA, and Flash Attention are supplied by the
# NVIDIA base image.  They are intentionally absent here so pip cannot replace
# NVIDIA's development wheels with public PyPI builds.
transformers==5.5.4
accelerate==1.14.0
kernels==0.14.1
safetensors==0.8.0
huggingface-hub==1.16.1
Pillow==12.1.1
fastapi==0.121.3
uvicorn==0.49.0
python-multipart==0.0.32
av==18.0.0
packaging==25.0
